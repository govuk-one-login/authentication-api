{
  "name": "api-tests",
  "version": "1.0.0",
  "author": "Government Digital Service",
  "description": "API tests for authentication-api",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "cucumber-js",
    "test:ci": "start-server-and-test run-local-auth-api 'http://localhost:4402' test",
    "run-local-auth-api": "../gradlew -p ../local-running run > auth-api-output.log",
    "pretty": "prettier --write \"*/**/*.{ts,json}\""
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "prettier": "^3.6.2",
    "start-server-and-test": "^2.1.2",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "typescript": "^5.8.3"
  }
}
